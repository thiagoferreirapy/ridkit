"use client";
import { FormEvent,useEffect,useState } from "react";import { Check,Edit3,Loader2,Plus,RefreshCw,Search,Trash2,X } from "lucide-react";import { useAdmin } from "@/components/admin-state";import { configs } from "@/components/admin-panel";import { ImageUploadField } from "@/components/image-upload-field";import { TableSkeleton } from "@/components/skeletons";
type Item=Record<string,any>;const inputClass="h-11 w-full rounded-xl border border-line bg-white px-3 text-sm outline-none focus:border-accent",button="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl px-4 text-sm font-semibold transition disabled:opacity-50";
async function json(response:Response){const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||"Não foi possível concluir");return body.data;}
export function AdminResource({label}:{label:string}){const config=configs[label],{adminFetch}=useAdmin(),[items,setItems]=useState<Item[]>([]),[total,setTotal]=useState(0),[loading,setLoading]=useState(true),[search,setSearch]=useState(""),[editing,setEditing]=useState<Item|null>(null),[open,setOpen]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
  const load=async(query="")=>{setLoading(true);try{const data=await json(await adminFetch(`/api/${config.resource}?limit=100${query?`&q=${encodeURIComponent(query)}`:""}`));setItems(data.items);setTotal(data.pagination?.total||data.items.length);}catch(reason){setMessage(reason instanceof Error?reason.message:"Erro ao carregar");}finally{setLoading(false);}};useEffect(()=>{load();},[config.resource]);
  const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setBusy(true);setMessage("");const values=Object.fromEntries(new FormData(event.currentTarget)),body=Object.fromEntries(Object.entries(values).map(([key,value])=>[key,value===""?null:value]));try{await json(await adminFetch(`/api/${config.resource}${editing?`/${editing.id}`:""}`,{method:editing?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}));setOpen(false);setEditing(null);setMessage("Alterações salvas.");await load(search);}catch(reason){setMessage(reason instanceof Error?reason.message:"Erro ao salvar");}finally{setBusy(false);}};
  const remove=async(item:Item)=>{if(!confirm(`Desativar ${item.name||item.title||item.code||`#${item.id}`}?`))return;try{await json(await adminFetch(`/api/${config.resource}/${item.id}`,{method:"DELETE"}));await load(search);}catch(reason){setMessage(reason instanceof Error?reason.message:"Erro ao excluir");}};
  return <section className="mt-7 space-y-4"><div className="flex flex-col gap-4 rounded-2xl border border-line bg-white p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">{config.title}</h2><p className="mt-1 text-xs text-muted">{config.description} · {total} registros</p></div><button onClick={()=>{setEditing(null);setOpen(true);}} className={`${button} bg-dark text-white`}><Plus size={16}/>Novo registro</button></div><div className="card overflow-hidden"><div className="flex items-center gap-3 border-b border-line p-4"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load(search)} className="h-9 min-w-0 flex-1 text-sm outline-none" placeholder={`Buscar em ${label.toLowerCase()}...`}/><button onClick={()=>load(search)} className={`${button} bg-canvas`}><RefreshCw size={14}/>Buscar</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="bg-canvas text-muted"><tr>{config.columns.map(([,title])=><th className="p-4 font-medium" key={title}>{title}</th>)}<th className="p-4 text-right font-medium">Ações</th></tr></thead><tbody>{items.map(item=><tr className="border-t border-line" key={item.id}>{config.columns.map(([key,,format])=><td className="max-w-[280px] truncate p-4" key={key}>{format(item[key],item)}</td>)}<td className="p-3"><div className="flex justify-end gap-2"><button title="Editar" onClick={()=>{setEditing(item);setOpen(true);}} className="rounded-lg border border-line p-2"><Edit3 size={14}/></button><button title="Desativar" onClick={()=>remove(item)} className="rounded-lg border border-line p-2 text-danger"><Trash2 size={14}/></button></div></td></tr>)}{!loading&&!items.length&&<tr><td colSpan={config.columns.length+1} className="p-8 text-center text-muted">Nenhum registro encontrado.</td></tr>}</tbody></table>{loading&&<div className="p-4"><TableSkeleton rows={5} columns={config.columns.length+1}/></div>}</div></div>{message&&<p className="text-sm text-muted">{message}</p>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/55 p-4"><form onSubmit={submit} className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">{editing?"Editar":"Novo"} {config.title.toLowerCase()}</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{config.fields.map(field=>field.type==="image"?<ImageUploadField key={`${editing?.id||"new"}-${field.name}`} name={field.name} label={field.label} defaultValue={editing?.[field.name]||""} required={field.required} wide/>:<label className="text-xs font-semibold" key={field.name}>{field.label}{field.type==="select"?<select name={field.name} defaultValue={editing?.[field.name]??field.options?.[0]?.[0]} className={`mt-2 ${inputClass}`}>{field.options?.map(([value,title])=><option value={value} key={value}>{title}</option>)}</select>:<input name={field.name} required={field.required} type={field.type||"text"} defaultValue={editing?.[field.name]??""} className={`mt-2 ${inputClass}`}/>}</label>)}</div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className={`${button} border border-line`}>Cancelar</button><button disabled={busy} className={`${button} bg-accent text-white`}>{busy?<Loader2 className="animate-spin" size={16}/>:<Check size={16}/>}Salvar</button></div></form></div>}</section>;
}
