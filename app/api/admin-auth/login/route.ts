import { verifyPassword } from "@/lib/auth";import { adminById,findAdmin,issueAdminRefresh,markAdminLogin,setAdminCookies } from "@/lib/admin-auth";import { apiError,ok } from "@/lib/api";import { assertRateLimit } from "@/lib/rate-limit";import { audit } from "@/lib/audit";
export const runtime="nodejs";export async function POST(request:Request){try{assertRateLimit(request,"admin-login",5,15*60_000);const body=await request.json(),admin=findAdmin(String(body.email||"").trim());if(!admin||!verifyPassword(String(body.password||""),admin.password_hash))return Response.json({error:"Credenciais administrativas inválidas"},{status:401});markAdminLogin(admin.id);const user=adminById(admin.id)!;audit(user.id,"login","admin_session",user.id);await setAdminCookies(user,issueAdminRefresh(user.id,body.remember===true));return ok({user});}catch(error){return apiError(error);}}
