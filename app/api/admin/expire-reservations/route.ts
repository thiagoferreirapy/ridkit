import { getDb } from "@/lib/db";
import { apiError,ok } from "@/lib/api";
import { requireAdmin } from "@/lib/admin-auth";
import { audit } from "@/lib/audit";

export const runtime="nodejs";
export async function POST(){const db=getDb();try{const admin=await requireAdmin(["admin","manager"]);const orders=db.prepare("SELECT id,coupon_id FROM orders WHERE status='pending' AND payment_status='pending' AND pix_expires_at IS NOT NULL AND datetime(pix_expires_at)<=CURRENT_TIMESTAMP").all() as {id:number;coupon_id:number|null}[];db.exec("BEGIN IMMEDIATE");for(const order of orders){const items=db.prepare("SELECT variant_id,quantity FROM order_items WHERE order_id=?").all(order.id) as {variant_id:number;quantity:number}[];for(const item of items){db.prepare("UPDATE product_variants SET reserved_stock=MAX(0,reserved_stock-?) WHERE id=?").run(item.quantity,item.variant_id);const state=db.prepare("SELECT stock,reserved_stock FROM product_variants WHERE id=?").get(item.variant_id) as {stock:number;reserved_stock:number};db.prepare("INSERT INTO inventory_movements(variant_id,order_id,admin_id,movement_type,quantity,stock_after,reserved_after,reason) VALUES(?,?,?,'release',?,?,?,?)").run(item.variant_id,order.id,admin.id,-item.quantity,state.stock,state.reserved_stock,"Liberação de reserva expirada pela rotina de segurança");}db.prepare("UPDATE orders SET status='cancelled',payment_status='failed',payment_updated_at=CURRENT_TIMESTAMP,updated_at=CURRENT_TIMESTAMP WHERE id=?").run(order.id);db.prepare("INSERT INTO order_events(order_id,status,title,description) VALUES(?,'cancelled','Reserva expirada','Reserva liberada automaticamente pela rotina de segurança.')").run(order.id);if(order.coupon_id)db.prepare("UPDATE coupons SET used_count=MAX(0,used_count-1) WHERE id=?").run(order.coupon_id);}audit(admin.id,"expire_reservations","orders",undefined,{orders:orders.length});db.exec("COMMIT");return ok({expired_orders:orders.length});}catch(error){if(db.isTransaction)db.exec("ROLLBACK");return apiError(error);}}
